# 1 "t1ac.c"
# 1 "<built-in>"
# 1 "<command-line>"
# 1 "t1ac.c"

after (char * x, char * y ) : call (char * foo3(char *, int)) && result(y) && args(x, int) {
        printf("after calling foo3, parameter = %s, result = %s \n", x, y);
}
